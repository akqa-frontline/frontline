const FILE_REGEX = /\.(gif|jpg|jpeg|png|svg)$/;

const SVG_INLINE_REGEX = /\.inline.svg$/;
const SVG_BUNDLE_REGEX = /\.bundle.svg$/;
const SVG_COMPONENT_REGEX = /\.component.svg$/;

export { FILE_REGEX, SVG_INLINE_REGEX, SVG_BUNDLE_REGEX, SVG_COMPONENT_REGEX };
